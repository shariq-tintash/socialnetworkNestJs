import mongoose from 'mongoose';
const Schema = mongoose.Schema;

export const ModeratorSchema = new Schema({
  email: {
    type: String,
    required: true,
  },
  password: {
    type: String,
    required: true,
  },
  name: {
    type: String,
    required: true,
  },
});
